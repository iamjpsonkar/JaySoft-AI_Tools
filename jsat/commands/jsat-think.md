---
description: Think carefully before acting — IThinking shortcut.
---

Before doing anything, use jsat__ithinking_plan with task="$ARGUMENTS" to clarify intent, check assumptions, and decompose the work. Show the plan and ask for confirmation before proceeding.

HOW TO RESPOND: Actually invoke the tool(s) described above, then reply with a direct, useful answer built from the result — interpret it for the user in plain language. Do not merely describe what the tool does, and do not echo raw JSON. If a tool returns an intermediate artifact (e.g. an optimized prompt), use it to finish the task rather than presenting it as the final answer.
